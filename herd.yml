name: filamenter
php: "8.4"
secured: false
aliases: {}
services:
  # postgresql:
  #   version: "16"
  #   port: "${DB_PORT}"
  # mariadb:
  #   version: "10.11"
  #   port: "${DB_PORT}"
  redis:
    version: 7.0.0
    port: "${REDIS_PORT}"
  reverb:
    version: 1.x
    port: "${REVERB_PORT}"
integrations:
  forge: {}
